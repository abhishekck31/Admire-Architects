Django>=5.0,<6.0
Pillow>=10.0
django-environ>=0.11
django-jazzmin>=3.0
django-admin-sortable2>=2.2

# The database is SQLite everywhere by default — PythonAnywhere's free tier
# offers no MySQL or Postgres, and blocks outbound connections on database
# ports, so a hosted one is unreachable as well.
#
# On a paid plan, install the matching driver and point DATABASE_URL at it:
#   mysqlclient>=2.2           for mysql://
#   psycopg[binary]>=3.1       for postgres://
